<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.tp.admin.dao.TerDao">


	<select id="findAllTerIdAndTitle" resultType="java.util.Map">
        SELECT t.id , ti.title
 		FROM
		ter AS t INNER JOIN ter_info AS ti ON t.id = ter_id
		WHERE
		t.deleted = 0
    </select>

    <select id="listTerCityId" resultType="java.lang.Integer">
        SELECT
		city
		FROM
		ter AS t
		GROUP BY t.city
    </select>

    <select id="terInfoSearch" parameterType="com.tp.admin.data.parameter.WxMiniSearch" resultMap="TerInfoDTOMap">
		SELECT
		t.id ,
		ti.online ,
		ti.title ,
		ti.status ,
		ti.cover ,
		ti.address ,
		ti.geo_code ,
		t.code
		FROM
		ter AS t INNER JOIN ter_info AS ti ON t.id = ti.ter_id
		WHERE
		ti.deleted = 0
		<if test="terId !=null and terId != 0">
			AND t.id = #{terId}
		</if>
		<if test="cityCode !=null and cityCode != 0">
			AND t.city = #{cityCode}
		</if>
		<if test="online != null">
			AND ti.online = #{online}
		</if>
		<if test="status != null and status == 1">
			AND ti.status = #{status}
		</if>
		<if test="ids != null and ids.size != 0">
			AND
			t.id IN
			<foreach collection="ids" index="index" item="item" separator="," open="(" 			close=")">
				#{item}
			</foreach>
		</if>
		ORDER BY ti.id ASC
		<if test="offset != null and pageSize != null and pageSize != 0">
			LIMIT #{offset},#{pageSize}
		</if>
	</select>

	<select id="cntTerInfoSearch" parameterType="com.tp.admin.data.parameter.WxMiniSearch"
			resultType="java.lang.Integer">
		SELECT
		COUNT(t.id)
		FROM
		ter AS t INNER JOIN ter_info AS ti ON t.id = ti.ter_id
		WHERE
		ti.deleted = 0
		<if test="terId !=null and terId != 0">
			AND t.id = #{terId}
		</if>
		<if test="cityCode !=null and cityCode != 0">
			AND t.city = #{cityCode}
		</if>
		<if test="online != null">
			AND ti.online = #{online}
		</if>
		<if test="status != null and status == 1">
			AND ti.status = #{status}
		</if>
		<if test="ids != null and ids.size != 0">
			AND
			t.id IN
			<foreach collection="ids" index="index" item="item" separator="," open="(" 			close=")">
				#{item}
			</foreach>
		</if>
	</select>

    <select id="findRelatedTerByPartnerId" resultType="java.lang.Integer">
		SELECT tpp.ter_id
		FROM
		ter_pk_partner AS tpp INNER JOIN ter AS t ON tpp.ter_id = t.id
		WHERE
		tpp.partner_id = #{partnerId}
		AND
		tpp.enable = 1
		AND
		t.deleted = 0
	</select>

	<select id="findTerStartInfo" parameterType="java.lang.Integer" resultMap="AdminTerPropertyDTOMap">
		SELECT t.id AS terId,
				t.ip,
				t.port,
				atp.net_method,
				atp.video_control,
				atp.bubble_limit,
				atp.ter_clientversion,
				atp.ter_busi_mode,
				atp.ter_model,
				atp.ter_remark,
				atp.high_limit,
				atp.wide_limit,
				atp.start_online
        FROM admin_ter_property AS atp
        INNER JOIN ter AS t
        ON t.id=atp.ter_id
				WHERE t.id=#{terId}
	</select>

    <resultMap id="TerInfoDTOMap" type="com.tp.admin.data.dto.TerInfoDTO" >
		<id column="id" property="id" jdbcType="INTEGER" />
		<id column="code" property="code" jdbcType="VARCHAR" />
		<id column="address" property="address" jdbcType="VARCHAR" />
		<id column="geo_code" property="geoCode" jdbcType="VARCHAR" />
		<id column="title" property="title" jdbcType="VARCHAR" />
		<result column="online" property="online" jdbcType="BIT" />
		<result column="status" property="status" jdbcType="SMALLINT" />
	</resultMap>

	<resultMap id="AdminTerPropertyDTOMap" type="com.tp.admin.data.dto.AdminTerPropertyDTO">
		<id column="terId" property="terId" javaType="INTEGER"/>
		<id column="ip" property="ip" jdbcType="VARCHAR" />
		<id column="port" property="port" jdbcType="VARCHAR" />
		<id column="net_method" property="netMethod" jdbcType="INTEGER"/>
		<id column="video_control" property="videoControl" jdbcType="VARCHAR"/>
		<id column="bubble_limit" property="bubbleLimit" jdbcType="INTEGER"/>
		<id column="ter_clientversion" property="terClientVersion" jdbcType="VARCHAR"/>
		<id column="ter_busi_mode" property="terBusiMode" jdbcType="INTEGER"/>
		<id column="ter_model" property="terModel" jdbcType="VARCHAR"/>
		<id column="ter_remark" property="terRemark" jdbcType="VARCHAR"/>
		<id column="high_limit" property="highLimit" jdbcType="SMALLINT"/>
		<id column="wide_limit" property="wideLimit" jdbcType="SMALLINT"/>
		<id column="start_online" property="startOnline" jdbcType="INTEGER"/>
	</resultMap>

</mapper>